---
description: NoteWise project conventions (Mark 2–3)
alwaysApply: true
---

# NoteWise

AI medical voice scribe **demo**. **NOT for clinical use.** Include that disclaimer in user-visible API copy you add.

## Secrets and local-only paths

- Never commit `.env`, API keys, or PHI.
- Never stage or commit `CLAUDE.md` or `.claude/` (gitignored).

## Mark 3 auth and imaging (env placeholders only in repo)

- `JWT_SECRET`, `JWT_ALGORITHM` (e.g. HS256), `ACCESS_TOKEN_EXPIRE_MINUTES` — required for `/auth/*` and Bearer-protected routes (`/chat`, `/scribe`).
- `MOONSHOT_API_KEY`, `MOONSHOT_BASE_URL` (OpenAI-compatible, e.g. `https://api.moonshot.ai/v1`), `MOONSHOT_VISION_MODEL` — for `/imaging/classify` (replaces local ViT).

## Environment

- `conda activate notewise` before backend installs, `uvicorn`, tests, and Docker builds that use local Python.

## Branch

- Mark 1/2/3 app work: `dev/testing` unless you branch explicitly.

## Ports

- Backend: `http://127.0.0.1:8000`
- Frontend (Vite): `http://localhost:5173`
- Vite proxy: `/api` → backend (when configured).

## Logs

- No PHI in logs, analytics, or committed files.
