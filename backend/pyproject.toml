[project]
name = "notewise-backend"
version = "0.1.0"
description = "NoteWise API — AI Medical Voice Scribe (NOT for clinical use)"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
